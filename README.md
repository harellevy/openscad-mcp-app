# openscad-mcp-app

An MCP (Model Context Protocol) app for iterating on [OpenSCAD](https://openscad.org/) models inline in chat — write OpenSCAD code, render it, view the result, and refine it iteratively while 3D modeling.

## Status

Project scaffolding in progress.
